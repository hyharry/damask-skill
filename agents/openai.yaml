interface:
  display_name: "DAMASK Skill"
  short_description: "Prepare DAMASK runs and Python workflows safely"
  default_prompt: "Use $damask-skill to retrieve a relevant input or Python reference, adapt it for my environment, and keep compatibility and scientific assumptions explicit."
