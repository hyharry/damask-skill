interface:
  display_name: "DAMASK Skill"
  short_description: "Retrieve examples and safely prepare DAMASK runs"
  default_prompt: "Use $damask-skill to retrieve a relevant reference case, prepare a version-appropriate DAMASK command, and keep scientific assumptions explicit."
